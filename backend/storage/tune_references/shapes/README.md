# 甲型参考素材

将甲型参考图片放在本目录，并在 `manifest.json` 的 `shapes` 数组中登记。

```json
{
  "id": "almond",
  "label": "杏仁甲",
  "image": "almond.png",
  "prompt": "将全部指甲调整为参考图中的杏仁甲型"
}
```

要求：

- 文件名可以使用当前清单中的中文名称，必须与 `manifest.json` 完全一致。
- 建议为至少 1024x1024 的 PNG、JPG 或 WebP。
- 背景干净，甲型轮廓清晰。
- 不要添加文字、水印或复杂装饰。
